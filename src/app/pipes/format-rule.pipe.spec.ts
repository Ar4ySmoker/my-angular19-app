import { FormatRulePipe } from './format-rule.pipe';

describe('FormatRulePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRulePipe();
    expect(pipe).toBeTruthy();
  });
});
